apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  labels:
    app: postgres
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      volumes:
      - name: postgres-data
        persistentVolumeClaim:
          claimName: postgres
      containers:
      - name: postgres
        image: postgres:18
        envFrom:
        - secretRef:
            name: postgres
        volumeMounts:
        - name: postgres-data
          mountPath: /var/lib/postgresql