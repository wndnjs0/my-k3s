apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: keepout
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/wndnjs0/my-k3s.git
    targetRevision: HEAD
    path: manifests/keepout
  destination:
    server: https://kubernetes.default.svc
    namespace: keepout
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true